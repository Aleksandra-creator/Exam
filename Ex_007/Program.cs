int sf1 = 1;
int sf2 = 2;
int sfd = 5;
int dist = 100;
int count = 0;

while(dist < 10);
{
    bool friend = true;
    if(friend = true);
    {
        int time = dist / (sf2 + sfd);
        bool friend = true;

    }
    if(friend = false);
    {
        int time = dist / (sf1 + sfd);
        bool friend = false;
    }
    Console.WriteLine(count);
    count++;
}